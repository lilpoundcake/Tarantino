ChainInfo — interface { id, entityId, residues[] }
ElementInfo — interface { chainId, entityId, entityType, moleculeName, compIds[], residueCount, atomCount }
StructureMeta — interface { name, organism, method, resolution, description }
ViewerSlot — 'primary' | 'secondary'
useStructureStore — Zustand hook exposing: plugin, secondaryPlugin, loadTargetSlot, chains, secondaryChains, elements, activeChainId, fileName, secondaryFileName, isLoading, error, meta, focusedChainId, focusedCategory, cameraSyncEnabled, clearAllSignal, setPlugin(), setSecondaryPlugin(), setLoadTargetSlot(), setChains(), setSecondaryChains(), setElements(), setActiveChain(), setFileName(), setSecondaryFileName(), setLoading(), setError(), setMeta(), setFocusedChain(chainId, category?), setCameraSyncEnabled(), fireClearAll(), reset()
